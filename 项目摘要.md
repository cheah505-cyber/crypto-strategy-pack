# crypto-strategy-pack 项目摘要

> **定位：** 本仓专属长期记忆与结构化回溯备忘录，不是聊天记录或实时交易数据源。
> **读法：** 状态栏 → Tag 路由 → 最新回溯 → 下次优先。
> **写法：** 每完成一段可独立验收的工作，追加目标 / 方案 / 结果 / 痛点和已登记 Tags。
> **最后整理：** 2026-07-20 18:10（+08:00）

## 0. 状态栏（30 秒）

| 项 | 当前 |
| --- | --- |
| 阶段 | ETH 4H ADX 自适应永续策略处于纸面交易与持续验证阶段 |
| 当前目标 | 保持纸面交易状态可追溯，并以既有验证门禁约束任何实盘升级 |
| 实时事实源 | `paper_trade/state.json`；成交与权益分别见对应 CSV |
| 暂停点 | 项目记忆入口已建立；策略参数与实盘条件未因本次文档升级改变 |
| 下一验收 | 后续可验收工作必须同步回溯，并通过对应最小测试或策略验证 |

## 1. Tag 路由（精准检索）

| Tag | 含义 | 典型落点 |
| --- | --- | --- |
| `#目标-纸面交易` | 当前策略的纸面运行与信号验证 | `paper_trade/state.json`、workflows |
| `#目标-实盘门禁` | 实盘前置条件与暂停条件 | `DECISIONS.md` |
| `#方案-ADX自适应` | Donchian + ADX + ATR% 策略路径 | `STRATEGY.md`、`PARAMETERS.md` |
| `#方案-验证管线` | preflight、sanity、样本外与 walk-forward | `VALIDATION.md` |
| `#结果-自包含` | 仓库自包含检查 | `tests.test_project_self_contained` |
| `#痛点-未来函数` | 时间对齐或前视偏差 | `VALIDATION.md`、回测代码 |
| `#痛点-过拟合` | 参数搜索与多重比较风险 | `INDEX.md`、`DECISIONS.md` |
| `#痛点-状态漂移` | 文档快照与实时 state 冲突 | `paper_trade/state.json` |
| `#痛点-入口命令` | 环境没有 `python` 命令别名 | `AGENTS.md` 最小验证 |
| `#边界-非实盘` | paper trade 不等于实盘授权 | `DISCIPLINE.md`、`DECISIONS.md` |

## 2. 回溯备忘录（新在上）

### 2026-07-20 · 清理 GitHub Agent 工具绑定 `#结果-自包含` `#痛点-入口命令` `#痛点-状态漂移`

- **目标：** 让 GitHub 与 Windows 工作区共享最新纸面状态，并让任意 Agent 无需指定插件即可接管。
- **方案：** 合并远端两笔自动状态提交；移除未安装的 Serena MCP 和 Claude 专用命令/规则载体；把历史实施方案归档，修正根目录路径、数据范围和 CI 顺序说明。
- **结果：** 现行规则只由 `AGENTS.md`、`INDEX.md`、`TOOLS.md`、`VALIDATION.md` 与 `DISCIPLINE.md` 定义；GitHub Actions 的 Linux/Telegram 依赖仍保留且与 Windows 本机边界分离。
- **痛点：** 仓库专用 Agent 配置没有安装声明或版本锁定，会让新机器误以为必须安装；以后由自包含测试禁止重新引入 `.claude` 或根级 MCP 配置。

### 2026-07-20 · 整理工具与回测队列入口 `#方案-验证管线` `#结果-自包含` `#痛点-入口命令`

- **目标：** 清除迁移缓存与失效入口，让 Windows Agent 能判断每个脚本的用途、依赖和副作用。
- **方案：** 新增 `TOOLS.md` 与 `loop/README.md`，移除 Ubuntu Bash 自动循环；修复两份报告脚本的硬编码路径，保留 CI 专用 Shell 并明确边界。
- **结果：** 日常、数据、验证、研究、测试网和 CI 工具已分类；脚本改用仓库相对路径和现有 `data\eth_usdt_4h.csv`；缓存和空目录不再属于项目结构。
- **痛点：** “没有被其他文件引用”不能证明脚本无用，许多研究工具本来就是直接入口；以后以 `TOOLS.md` 的能力分类和输入前置条件判断，而非按引用数删除。

### 2026-07-20 · 完成 Windows 接管迁移 `#方案-验证管线` `#结果-自包含` `#痛点-入口命令`

- **目标：** 移除 Ubuntu 专用环境路径，让 Windows Agent 可从仓库入口直接建立环境并验证策略。
- **方案：** 将现行命令统一到项目 `.venv\Scripts\python.exe`，修复 Windows 批处理入口，恢复误删的 `.codegraph/.gitignore`，并从 `requirements.txt` 重建运行环境。
- **结果：** Windows Python 3.13 虚拟环境已安装 numpy 2.5.1、pandas 2.3.3 和 ccxt 4.5.67；5 项自包含测试与 `backtests\adx_adaptive_perp_eth_4h.py` 完整 preflight/sanity/回测均通过。
- **痛点：** Ubuntu 虚拟环境不可跨系统复用；以后只提交依赖清单，不提交 `.venv`，新机器按 README 重建并运行两级验证。

### 2026-07-20 · 对齐远端纸面状态并发布项目记忆 `#目标-纸面交易` `#结果-自包含` `#痛点-状态漂移` `#痛点-入口命令`

- **目标：** 在不覆盖自动纸面交易状态的前提下，将本地仓库对齐到GitHub最新`master`，并发布项目专属记忆与可移植接管入口。
- **方案：** 先执行`git fetch origin --prune`并核对分支、工作区和双向提交差；确认远端35个提交只更新行情与纸面交易状态文件后，将本地`master`快进到`da8168b`，再从最新远端基线建立`agent/sync-project-memory`，只提交`AGENTS.md`、`INDEX.md`和本文件。
- **结果：** 本地实时状态已快进到远端最新基线，自动生成的数据文件未被文档改动覆盖；项目自包含5项测试、项目记忆检查与`git diff --check`均通过。发布采用独立分支和草稿PR，不直接写入自动运行的默认分支。
- **痛点：** 原`gh`令牌失效导致发布流程中断；通过GitHub官方设备授权恢复。以后发布前先验`gh auth status`，并始终先刷新远端，避免自动任务持续提交时从过期基线推送。

### 2026-07-18 · 建立项目专属记忆 `#目标-纸面交易` `#方案-验证管线` `#结果-自包含` `#痛点-状态漂移` `#痛点-入口命令`

- **目标：** 让后续 Agent 从仓库内恢复目标、方案、验证结果、失败经验和下一步，同时不把实时交易状态复制进长期摘要。
- **方案：** 新增本文件并接入 `AGENTS.md`、`INDEX.md`；详细策略、参数、决策、教训和流水仍留在原权威文件。
- **结果：** `python3 -m unittest tests.test_project_self_contained -v` 通过 5 项测试；本次只改变文档入口，不改变策略参数、状态或实盘条件。
- **痛点：** 原最小验证写 `python`，当前环境报 `command not found`；根因是命令别名不存在，已统一为 `python3`。余额、信号与绩效会持续变化，后续禁止复制到本摘要，必须回到 `paper_trade/state.json` 和 CSV 核验。

## 3. 已确认决定（长期）

- `paper_trade/state.json` 是纸面交易唯一实时状态；文档数字只作历史快照。
- paper trade 不等于实盘；实盘条件以 `DECISIONS.md` 为准。
- 参数变化必须经过独立样本外、敏感性和 walk-forward 验证。
- 费用、滑点与资金费率继续从 `utils/constants.py` 引用。

## 4. 下次优先

1. **动作：** 继续按既有 workflow 和纸面交易纪律积累可核验状态，不因本摘要改变策略。
   **验收：** `paper_trade/state.json`、成交 CSV、权益 CSV 与最近一次 workflow 结果之间无未解释冲突。
2. **动作：** 下一次形成稳定结果或踩坑时，在本节上方追加回溯。
   **验收：** 回溯包含四字段，Tags 已登记，结果带实际命令或路径。

## 5. 常用入口与验证

- 规则与接管：`AGENTS.md`、`INDEX.md`
- 策略与参数：`STRATEGY.md`、`PARAMETERS.md`
- 决策与风控：`DECISIONS.md`、`DISCIPLINE.md`
- 最小文档验证：`.\.venv\Scripts\python.exe -m unittest tests.test_project_self_contained -v`
- 完整策略检查：`.\.venv\Scripts\python.exe backtests\adx_adaptive_perp_eth_4h.py`

## 6. 核心风险（常亮）

- 前视偏差、参数过拟合和状态漂移仍是主要失效点；分别由验证门禁、决策纪律和实时状态文件约束。
- 本摘要不是实时交易面板，也不构成实盘授权。

## 7. 回溯追加模板

```markdown
### YYYY-MM-DD · 短标题 `#tagA` `#tagB`

- **目标：** …
- **方案：** …
- **结果：** …（命令、路径、产物或未验证原因）
- **痛点：** …（错误表现、根因或证据、规避方式；无新增则写“无新增”）
```
